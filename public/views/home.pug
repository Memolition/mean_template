<div ng-controller="home_controller">
    <article ng-repeat="post in blog">
        <h2>{{post.title}} <small>{{post.author}} - {{post.date}}</small></h2>
        <p>{{post.body}}</p>
    </article>
</div>